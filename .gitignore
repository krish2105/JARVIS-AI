.env
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
/build/
/dist/
.DS_Store

# runtime audio scratch files
*.wav

# personal memory contents — keep the directory, not what's written into it.
# preferences.md is intentionally NOT committed: it holds personal facts that
# Jarvis learns at runtime and must never be baked into the repo.
memories/*
!memories/.gitkeep

# logs
*.log

# node deps pulled in for @playwright/mcp
node_modules/
